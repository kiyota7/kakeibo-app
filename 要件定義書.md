# 家計簿アプリ「カケイボ」要件定義書

- 作成日: 2026年7月25日
- 作成者: (氏名を記入)
- バージョン: 1.0

---

## 1. はじめに

### 1.1 背景

日々の収入・支出を紙の家計簿やExcelで管理しようとしても、入力が続かなかったり、
「今月結局いくら使ったのか」「何にお金を使いすぎているのか」が一目でわからず
挫折してしまうという課題がある。そこで、収支を記録し、カテゴリ別・月別に自動集計できる、
シンプルな家計簿Webアプリケーションを開発する。

### 1.2 目的

本ドキュメントは、開発する家計簿アプリ「カケイボ」(以下、本システム)の機能・仕様を明確にし、
開発内容の認識をそろえることを目的とする。

### 1.3 対象読者

本システムの発注者(依頼者)、開発者、および課題の評価者。

### 1.4 想定発注シーン(ヒアリング内容)

本課題では、以下のような依頼を受けた想定で要件を整理する。

> 個人で家計管理をしたい依頼者から、「現金とカードの支出がバラバラで、月にいくら使ったのか
> 把握できていない。Excelは入力が面倒で続かない。日付やカテゴリ(食費・交通費など)で
> 後から見返せて、月ごとの集計が自動で出るような、シンプルな家計簿アプリが欲しい」
> という相談を受けた。

この相談内容から、以下の点を本システムが満たすべき条件として設定する。

- 収入・支出を、日付・金額・カテゴリを指定して簡単に登録できること
- 登録した内容を、日付やカテゴリで絞り込んで後から確認できること
- 月ごとの収支合計、カテゴリごとの支出内訳が自動で集計・表示されること
- 個人利用を前提とし、複雑な会員管理機能は持たないこと(シングルユーザー)

---

## 2. システム概要

### 2.1 システム名称

カケイボ(家計簿Webアプリ)

### 2.2 システムの位置づけ

Next.js(TypeScript)製のフロントエンドとRuby on Rails(APIモード)製のバックエンドで構成される
Webアプリケーション。データはバックエンドが提供するREST APIを介してMySQLデータベースに保存する
(詳細は[技術スタック](docs/技術スタック.md)参照)。ログイン機能は持たず、単一の利用者が
自分のPCから利用することを前提とする。

### 2.3 想定利用者

- 個人で家計管理をしたい学生・社会人

### 2.4 利用シーン

- 日々の買い物や支払いを、カテゴリを選んで記録する
- 月末に、その月の収支合計とカテゴリ別の内訳を確認する
- 「今月の食費だけ」「先月の交通費だけ」のように、条件を絞って過去の記録を確認する

---

## 3. ドキュメント構成

本要件定義書は全体の背景・合意事項をまとめるインデックスとし、
機能・画面・データ・非機能要件・技術スタックの詳細は以下の各ドキュメントに分割する。

| ドキュメント | 概要 |
|---|---|
| [機能一覧](docs/機能一覧.md) | 本システムが備える機能(F-01〜)とスコープ外項目の一覧 |
| [画面設計書](docs/画面設計書.md) | 画面一覧、画面構成要素、画面遷移 |
| [データベース設計書](docs/データベース設計書.md) | ER図、テーブル定義、保存形式(MySQL) |
| [非機能要件定義書](docs/非機能要件定義書.md) | 動作環境、性能、ユーザビリティ等の非機能要件 |
| [技術スタック](docs/技術スタック.md) | フロントエンド(Next.js)・バックエンド(Ruby on Rails)・データベース(MySQL)などの開発環境 |
| [インフラ構成](docs/インフラ構成.md) | AWS上のインフラ構成図・ディレクトリ構成(Terraform管理) |

---

## 4. 検収基準(受け入れ基準)

納品物が以下の基準をすべて満たすことを、検収完了の条件とする。

| No | 基準 | 確認方法 |
|---|---|---|
| A-01 | [機能一覧](docs/機能一覧.md)の機能がすべて仕様通りに動作する | 依頼者による実機操作確認 |
| A-02 | [非機能要件定義書](docs/非機能要件定義書.md)に記載の対応ブラウザで正常に動作する | 各ブラウザでの動作確認 |
| A-03 | 登録した収支データが、ブラウザを再読み込みしても保持されている | リロード後の表示確認 |
| A-04 | 日付・カテゴリでの絞り込み結果が、実際のデータと一致する | 実機操作確認 |
| A-05 | 月別集計・カテゴリ別集計の金額が、個々の取引データの合計と一致する | 実機操作確認(手計算との突合) |

上記が満たされない項目がある場合は、不具合として開発者へ差し戻し、再修正のうえ再度確認を行う。

---

## 5. 制約事項

- 単一利用者による利用を前提とし、会員登録・ログイン機能は持たない
- 同一データに対する複数人・複数端末からの同時編集(排他制御)には対応していない
- 対応ブラウザ以外(古いブラウザ等)での動作は保証しない
- 通貨は日本円(整数)のみを対象とする

---

## 6. 今後の拡張案(参考)

学校課題としての現バージョンでは対象外とするが、発展課題として以下が考えられる。

- 複数ユーザー対応(会員登録・ログイン、ユーザーごとのデータ分離)
- カテゴリ別集計のグラフ表示(円グラフ・棒グラフ)
- 予算設定機能(カテゴリごとの月間予算と実績の比較)
- CSVエクスポート・インポート

---

## 7. 用語集

| 用語 | 説明 |
|---|---|
| 取引 | 1件の収入または支出の記録(日付・金額・カテゴリ・メモ等を持つ) |
| カテゴリ | 取引を分類するための区分(食費・交通費・給与など) |
| 収支 | 収入と支出、およびその差引 |
| 月別集計 | 月単位で集計した収入合計・支出合計・差引 |
| REST API | フロントエンドとバックエンドがHTTP通信でデータをやり取りするための設計様式 |
| Ruby on Rails | Rubyでバックエンド(サーバー側)のアプリケーションを構築するためのフレームワーク |
| Next.js | React/TypeScriptでフロントエンド(画面)を構築するためのフレームワーク |
| MySQL | 本システムがデータの永続化に使用するリレーショナルデータベース管理システム(RDBMS) |

---

## 8. 承認

本要件定義書の内容について、双方合意のうえ以下に承認する。

| 役割 | 氏名 | 承認日 | 署名 |
|---|---|---|---|
| 発注者(依頼者) | (氏名を記入) | (記入) | |
| 受注者(開発者) | (氏名を記入) | (記入) | |
